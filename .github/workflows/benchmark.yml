name: Benchmark

on:
  schedule:
    - cron: "0 0 * * 0"  # Every Sunday at 00:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    name: Parse Benchmark
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install CodeRAG
        run: pip install -e '.[all]'

      - name: Clone test repository (Slim framework)
        run: git clone --depth 1 https://github.com/slimphp/Slim.git /tmp/bench-slim

      - name: Run CodeRAG parse
        run: coderag parse /tmp/bench-slim

      - name: Run CodeRAG info
        run: |
          cd /tmp/bench-slim
          coderag info | tee /tmp/benchmark-info.txt

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-${{ github.run_number }}
          path: /tmp/benchmark-info.txt
          retention-days: 90
